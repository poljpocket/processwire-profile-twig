<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta content="width=device-width, initial-scale=1" name="viewport">
    <title>{{ page.title }}</title>
    <meta name="robots" content="{{ config.debug == false ? 'index, follow' : 'noindex, nofollow' }}">
</head>
<body>
{% block content %}

{% endblock %}
</body>
</html>
